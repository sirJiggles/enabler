{
  "name": "enabler",
  "author": "Gareth Fuller",
  "license": "Apache-2.0",
  "description": "a 'simple' slack bot",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel",
    "bot": "cd packages/bot && yarn start",
    "bot-prod": "PRODUCTION=true yarn bot",
    "deploy": "lerna run package && lerna run up",
    "deploy-prod": "PRODUCTION=true yarn deploy"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
